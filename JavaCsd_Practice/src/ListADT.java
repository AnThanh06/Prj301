



// Link list danh sách liên kết 
// dataNode: tên của 1 lớp được dùng chính nó để làm kiểu dữ liệu
//chains:chuỗi;  
// interface
public interface ListADT<e>{
    void add(e element);
    
    boolean isEmpty();
    
    
}
